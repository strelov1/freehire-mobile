## 1. The screen

- [x] 1.1 `src/app/(tabs)/profile.tsx` — drop the two chip rows, keeping the editor row and its
      counts. Remove the styles and the label import they were the only users of.

## 2. Verification

- [x] 2.1 `npm run lint` and `npx tsc --noEmit` clean.
- [x] 2.2 Unit tests pass.
